//includes that is needed
//Constructor New_item::New_item(----)
//New_Item::Init(----)
//what does it do (function hit )
//exemple....->
/*bool New_item::hit(AbstractKart* kart, PhysicalObject* obj)
{
    bool was_real_hit = Flyable::hit(kart, obj);
    if(was_real_hit)
    {
        if(kart && kart->isShielded())
        {
            kart->decreaseShieldTime();
            return true;
        }
        else
        {
            m_has_hit_kart = kart != NULL;
            explode(kart, obj, //hit_secondary//false);
        }
    }
    return was_real_hit;
}  */
